<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ProteinActivityRootView"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:background="@color/strong_gray_color"
    tools:context=".activitites.OrderByImageActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/OrderByImageActivityToolbar"
        style="?actionBarSize"
        app:titleTextColor="@color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        app:navigationIcon="@drawable/ic_arrow_back_black_24dp"
        app:title="@string/OrderByImage" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">


            <ImageView
                android:id="@+id/ImageOfList"
                android:scaleType="fitXY"
                android:layout_width="match_parent"
                android:layout_height="@dimen/_500sdp"
                android:contentDescription="@string/todo" />


            <com.google.android.material.button.MaterialButton
                android:id="@+id/TakeImageButtonFromCamera"
                style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/_10sdp"
                android:layout_gravity="center"
                android:insetLeft="4dp"
                android:text="@string/takeImageFromCamera"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/OrderButton"
                style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="@dimen/_10sdp"
                android:insetLeft="4dp"
                android:text="@string/order_now"/>
        </LinearLayout>

    </ScrollView>
</LinearLayout>